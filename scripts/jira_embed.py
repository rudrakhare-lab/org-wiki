"""
jira_embed.py — Tier 1: embed tickets into raw/jira/embeddings.db (sqlite-vec).

Phase 5 deliverable. Default model: `sentence-transformers/all-MiniLM-L6-v2`
(local, free). Embeds `summary + description_text + resolution_text` per ticket.

Status: stub. Do not implement until Phases 1–4 are stable AND user approves
Phase 5 (master brief Checkpoint 7).
"""
