# Conwo backend dependencies
fastapi==0.115.5
uvicorn[standard]==0.32.1
anthropic==0.40.0
python-dotenv==1.0.1
pydantic==2.10.3
tomli==2.2.1        # Python <3.11 fallback for tomllib
PyYAML==6.0.3       # frontmatter parsing in wiki_propose_tools
python-frontmatter==1.1.0  # G35 — robust YAML frontmatter parsing
python-multipart>=0.0.12  # file upload support for /api/ingest/upload
pytest>=7.0
google-auth>=2.29.0
psycopg[binary]==3.2.3   # PostgreSQL driver (replaces sqlite3)
psycopg-pool==3.2.4      # connection pool for the single-DB pool in backend/db.py
prometheus-client==0.25.0  # /metrics endpoint (backend/metrics.py). NOTE: do NOT
                           # use prometheus-fastapi-instrumentator — its 8.x needs
                           # starlette>=1.0, which conflicts with FastAPI 0.115.5.
boto3>=1.34               # AWS Secrets Manager loader (backend/secrets_loader.py);
                          # only used in prod when CONWO_SECRET_ID is set.
