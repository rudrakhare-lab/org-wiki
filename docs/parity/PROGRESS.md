# Parity Closure Progress

Running log of which gaps from `GAPS.md` have been closed, when, and how to verify.

| Gap | Status | Closed | Eval signal | Tests | Notes |
|-----|--------|--------|-------------|-------|-------|
| **G06** — Answer-ID feedback prompt (api mode) | ✅ Closed | 2026-05-22 | Q19 should flip FAIL → PASS for api mode | `tests/test_orchestrator.py` (5 tests, all pass); hand-verified via `scratch/g06_verify.py` | Feedback block added to `deep_system_prompt.py` template. `_inject_answer_id` in orchestrator substitutes the real id, with a 3-line fallback block if the model omits the placeholder. Order: id computed first (from raw model output, placeholder intact), then injected, then logged with resolved text — JSONL stores what the user actually saw. |
| **G01** — Live Jira ticket lookup | ✅ Closed | 2026-05-22 | Q3p should flip FAIL → PASS when a fresh ticket key is wired into the eval | `tests/test_jira_live.py` (5 mocked tests, all pass); `tests/manual/g01_smoke.py` (live smoke against real Atlassian, all 4 cases pass) | New tool `jira_live_get_ticket` hits `/rest/api/3/issue/{key}` via urllib + Basic auth (JIRA_EMAIL + JIRA_API_TOKEN). Truststore injection required for corporate-proxy SSL (`scripts/lib/jira_client.py` pattern). Registry now expects 11 tools. Two real-Jira surprises caught — see report. |
| **G04** — Session-start operational posture | ✅ Closed | 2026-05-22 | Q21, Q22 should flip FAIL → PASS | `tests/test_operational_context.py` (3 tests, all pass); hand-verified via `tests/manual/g04_verify.py` (4 scenarios — fresh, stale, pending, empty-mirror) | New `backend/operational_context.py` reads `admin_api.get_sync_status()` (cached 5min), surfaces a compact `**Operational context:**` block only when something is "interesting" (stale mirror >36h, pending feedback >0, empty mirror). Prepended at top of `build_seed_message`. `deep_system_prompt.py` instructs the model how to react to each signal. PLAN.md's assumed `last_sync_line` shape was wrong — real data is JSON-encoded log lines; parser adapted to JSON-first / regex-fallback. |
| **G05** — Live Config Debug workflow completion | ✅ Closed | 2026-05-22 | Q11 (soft via Need: prompt), Q13, Q14 (mostly — see G32), Q15 should improve; Q12 → PASS | `tests/test_pms_live.py` (12 mocked tests, all pass); hand-verified via `tests/manual/g05_smoke.py` against real `.com` AND `.in` (6 cases). | Added 4 tools: `pms_list_offices`, `pms_list_criteria`, `pms_verify_buid`, `pms_diagnose_property`. Each wraps an existing `Session.*` method; `pms_diagnose_property` returns `{report_markdown, property, buid, server, value_found}`. Single-turn disambiguation via `**Need:**` pattern added to `deep_system_prompt.py`. Real PMS surprises caught at smoke time and corrected before close: (1) `fetch_roles` returns `buids` as list of dicts not strings → parser fixed; (2) `isAllBuids: true` flag means cross-tenant read access → handler now honors it with soft-vs-hard warning distinction. Truststore NOT needed for PMS hosts (verified at smoke). Registry size 11 → 15. |
| **G03** — Conversation context compaction | ✅ Closed | 2026-05-22 | Q23 should flip FAIL → PASS | `tests/test_conversation_compactor.py` (13 tests) + `tests/test_conversations.py` (4 new G03 tests) + `tests/test_preflight.py` (3 tests) + `tests/manual/g03_verify.py` (4 scenarios — under threshold, fresh compaction, cached reuse, no-API-key fallback) | New `backend/conversation_compactor.py` exposes `should_refresh`, `messages_to_summarize`, `summarize_old_turns`. Migration on `conversations` adds `compacted_summary TEXT` + `compaction_at_turn INTEGER` columns (idempotent). `backend/orchestrator.load_conversation_summary` reads/refreshes the summary at read time and `build_seed_message` prepends a `**Prior conversation summary**` block to the seed (Option A from discovery — not `prior_messages`, to avoid breaking role alternation). Compactor uses claude-haiku-4-5 by default (`ANTHROPIC_COMPACTOR_MODEL` env override). NO-API-KEY fallback returns empty summary → caller degrades to plain truncation. |
| **G08** — Conversational clarification primitive | ⏳ Open (P1, deferred) | — | Q11 PARTIAL (via single-turn workaround in G05) | — | Bridged by the single-turn `**Need:**` pattern added in G05 — the model can now ask a clarifying question, but it terminates the turn instead of waiting. Real multi-turn primitive (model emits a "need_clarification" tool result that the client surfaces and resumes the same agent loop) is a bigger lift. Defer until clear pilot demand. |
| **G29** — Feedback prompt missing in claude-code mode | ✅ Closed | 2026-05-22 | Q19 should now pass in claude-code mode too | `tests/test_orchestrator.py::test_run_single_shot_substitutes_answer_id_in_returned_text` (integration test, mocks provider + retrievers) | Appended the answer-template block in `system_prompt.py:load_system_prompt()` (as an extra section, not via CLAUDE.md edit). Mirrored the G06 sequence into `orchestrator.run_single_shot`. The helper `_inject_answer_id` is shared with `run_deep` — the 5 helper tests already cover all substitution edge cases. |
| **G30** — `jira_live_get_ticket` has no rate-limit backoff | ⏳ Open (P3, latent) | — | — | — | Discovered during G01 smoke. Real Atlassian budget ~400/window; pilot won't hit it. |
| **G33** — Compactor missing explicit Anthropic timeout | ✅ Closed | 2026-05-22 | (no eval signal — defensive) | `test_summarize_old_turns_returns_empty_on_timeout` + updated `test_summarize_old_turns_calls_anthropic_with_correct_args` (now asserts `timeout=30.0`) | `Anthropic(timeout=30.0)` in `backend/conversation_compactor.py`. TimeoutError swallowed by the existing exception path; caller falls back to plain truncation. |
| **Track A Sub-pass A+B** — Foundations + propose tools | ✅ Shipped | 2026-05-22 | None yet — apply path still no-op (G07 still open until Sub-pass C). | `tests/test_track_a_foundations.py` (13 tests) + `tests/test_wiki_propose_tools.py` (21 tests) + updated registry test for 19-tool count. 178/178 total. | **Sub-pass A:** typed proposal store with `proposal_type` + `applied_at`/`applied_by` + `suggested_companion_edit` + `validation_log`; startup WARN counts legacy_text; `wiki_read_page` disk-fallback for unindexed paths; `backend/file_locks.py` (POSIX flock context managers); deleted 3 Obsidian artifacts + added `_is_obsidian_artifact_at_root` filter. **Sub-pass B:** 4 new structured propose tools in `backend/tools/wiki_propose_tools.py` — `wiki_propose_new` (allowlist: concepts/, cross-module/, decisions/, answers/, sources/), `wiki_propose_edit` (str_replace; old_string unique; Q1 advisory companion_edit on depends_on/used_by; Q8 AUTO-block refuse with named owner script), `wiki_propose_append` (log.md format-validated), `wiki_propose_multi_edit` (atomicity at apply time). Registry 16 → 19. Permissions: all 4 require `contributor`. Old free-text `wiki_propose_edit` REMOVED; existing pending records auto-normalize to `proposal_type="legacy_text"`. |
| **G07** — wiki_propose_edit admin-apply now writes | ✅ Closed | 2026-05-22 | Admin apply flow now actually mutates wiki/ on disk for all 4 typed proposal types. | `tests/test_wiki_apply.py` (23 tests covering happy path / stale / lock contention / multi_edit atomicity + rollback / deadlock prevention / legacy_text / integration) + `tests/manual/track_a_verify.py` (6 end-to-end scenarios). 203/203 total. | New `backend/wiki_apply.py` provides 4 writer functions (`apply_new`, `apply_edit`, `apply_append`, `apply_multi_edit`) all using `locked_write` / `locked_read_write` from `backend/file_locks.py`. `admin_api.apply_wiki_proposal` rewritten to dispatch by `proposal_type` + handle legacy_text refusal. New `/admin/wiki/proposals/{id}/mark-applied` endpoint for legacy_text completion. HTTP status codes mirror result codes (404 not_found, 409 stale_proposal, 422 legacy_text_refused). Multi-edit atomicity: 2-pass validate-then-write with sorted-path lock acquisition (deadlock prevention) and content snapshot-based rollback on partial failure. |
| **G14** — Save-answer-as-wiki-page | ✅ Closed | 2026-05-22 | "Save this answer" propose → apply works end-to-end (see Scenario 1 of track_a_verify.py). | (covered by G07 tests + integration test) | Closed by `apply_new` writer + existing `wiki_propose_new` tool. The model proposes; the admin applies; the file lands on disk under `concepts/`, `cross-module/`, `decisions/`, `answers/`, or `sources/` per the allowlist. |
| **G16** — Atomic multi-file edit | ✅ Closed | 2026-05-22 | Multi-edit applies all-or-none with rollback on partial failure (see Scenario 4 of track_a_verify.py and `test_apply_multi_edit_rollback_on_write_failure`). | (covered by G07 tests) | Closed by `apply_multi_edit` writer. Two-pass validate-then-write, sorted-path lock acquisition for deadlock prevention, snapshot-based rollback on partial failure. Rollback status surfaced in response (`clean`/`partial`/`failed`). |
| **G34** — Q1 advisory regex misses block-style YAML lists | ✅ Closed | 2026-05-25 | — | `test_propose_edit_q1_companion_picks_up_block_style_yaml`, `test_propose_multi_edit_companion_works_across_mixed_styles`, `test_propose_edit_q1_handles_trailing_whitespace_and_tabs` | `_parse_list` now uses `yaml.safe_load` so inline `[a, b]` and block `- a` shapes both produce the same set. Defensive: any YAML parse error returns None and the advisory falls through. |
| **G35** — Frontmatter parser fragile on body `---` separators | ✅ Closed | 2026-05-25 | — | `test_propose_new_body_with_horizontal_rule_does_not_confuse_parser` | Switched `_extract_frontmatter` to `python-frontmatter==1.1.0`. Body-level `\n---\n` horizontal rules no longer poison parsing — the library only consumes the leading block. Added `python-frontmatter==1.1.0` + `PyYAML==6.0.3` to `requirements-backend.txt`. |
| **G38** — Feedback marker reserved-namespace refusal | ✅ Closed | 2026-05-25 | — | 4 new tests in `test_wiki_propose_tools.py` (one per tool) | New `_feedback_marker_error` check refuses any proposal whose new content embeds `<!-- feedback:ID -->` — the marker is reserved for `scripts/apply_feedback.py`'s idempotency check and a smuggled value would silently mask a real feedback application. For multi_edit, refusal fires if any edit's new_string contains the marker. |
| **G36** — `apply_multi_edit` releases per-file locks between files | ⏳ Open (P3, latent) | — | — | — | Discovered during Sub-pass C. Pilot is 1 admin so the window is microseconds. Refactor to `ExitStack`-managed all-locks-then-write when multi-admin pilot demands it. ~2h. |
| **G37** — `backend/api.py` at 872 lines needs split | ⏳ Open (P3) | — | — | — | Discovered during Sub-pass C. Split into `api_query.py` / `api_conversations.py` / `api_admin.py` when the file hits ~1200 lines or a contributor complains. ~half-day. |
| **Track A Sub-pass D** — Eval + harden | ✅ Shipped | 2026-05-22 | Q31 added to parity eval (api-only); stress + rollback harnesses pass cleanly. | `tests/manual/track_a_stress.py` (B.2: lock contention at N=2/5/10/20 → 1 success + N-1 stale, 0 exceptions across all N; B.3: 5-file multi_edit rollback under forced IO fail → 100/100 clean across 100 iterations). `tests/test_admin_wiki_proposals.py::test_mark_applied_triggers_reindex` added. **204/204** tests passing. | Mark-applied now triggers `wiki_retriever.rebuild_index` so admin's manual edits land in the searchable index. Q31 (api-only) tests "save correction as proposal" pattern. `docs/parity/TRACK_A.md` created (~360 lines) — durable architecture/runbook reference. Stress test surfaced 2 of its own design bugs (per-thread unique old_strings → no real contention; macOS path-resolution mismatch hiding the patch) — both fixed inline. G36/G37 filed for latent issues from Sub-pass C. B.5 parity eval against live API skipped — ANTHROPIC_API_KEY not yet configured. |
| **G24** — Anthropic model is hard-coded | ✅ Closed | 2026-05-22 | (no eval signal — config) | `tests/test_deep_query_config.py` (4 tests: default, env override, invalid value raises, compactor model env) | `ANTHROPIC_MODEL` env var, default `claude-sonnet-4-6`. `ANTHROPIC_COMPACTOR_MODEL` env var, default `claude-haiku-4-5`. Both documented in `.env.example`. |
| **G25** — `/query/stream` endpoint name is misleading | ✅ Closed | 2026-05-22 | (no eval signal — UX clarity) | — (docstring change) | Documented (option B), not renamed — frontend at `frontend/src/app/core/api.service.ts:324` hardcodes the path. Endpoint docstring now explicitly says it only streams claude-code mode, with a TODO marker for the G02 split. |
| **G10** — 8-round tool-loop cap | ✅ Closed | 2026-05-22 | Complex PMS-debug queries no longer force-synthesized | `tests/test_deep_query_config.py` (default = 12, env override, invalid value) | Default bumped 8 → 12; configurable via `MAX_TOOL_ROUNDS` env. **Needs eval validation when API key arrives** — flagged in `.env.example`. |
| **G09** — Large tool output continuation | ✅ Closed | 2026-05-22 | Q25 (long comment thread) should flip FAIL → PASS | `tests/test_tool_pagination.py` (5 jira tests) | `jira_get_ticket` accepts `description_offset` / `comments_offset` / `field_chunk_size`; returns `_total_length` / `_has_more` / `_next_offset` per field. Default behavior unchanged — `description_text` and `comments_text` keep their pre-G09 semantics; backwards-compat test confirms callers of `format_preflight_tickets` keep working. |
| **G20** — `view_range` / paged file reads | ✅ Closed | 2026-05-22 | (no current eval signal — latent risk) | `tests/test_tool_pagination.py` (5 wiki_read_page tests) | Closed in the same edit as G09. `wiki_read_page` accepts `offset` / `limit`; full-content default preserved when no limit is passed. |
| **G13** — wiki_grep with literal-match semantics | ✅ Closed | 2026-05-22 | Q26-style power-user "which pages cite X?" queries | `tests/test_wiki_grep.py` (6 tests: literal, regex, path_glob, max_matches truncation, invalid regex, missing pattern) | New `wiki_grep` tool (registry size 15 → 16). Iterates `wiki_retriever.all_paths()`; returns hits with ±2-line context. `path_glob` uses fnmatch; `max_matches` caps at 50 by default. Complements `wiki_search` (ranked) with deterministic completeness semantics. |
| **G31** — Weak grep in `admin_api.get_sync_status` | ✅ Closed | 2026-05-22 | Q21 stale-mirror branch now honest | `tests/test_operational_context.py` — 3 new G31 tests + 3 existing tests updated for new dict shape (6 total, all pass) | `get_sync_status()` now returns `most_recent_successful_sync` (last `ALL DONE:` INFO line) as the authoritative timestamp. `last_sync_line` preserved as back-compat alias. `operational_context.py` prefers the new field and emits a harder warning when logs exist but no successful sync is ever found. Real-world fix to the recent-ERROR / old-SUCCESS false-negative caught in G04. |
| **G32** — `pms_list_offices` returns HTTP 500 on `.com` mis-security host for at least `genpactindia-GInd` | ⏳ Open (P2) | — | Q14 blocked | — | Discovered during G05 smoke. Auth/provisioning issue on the mis-security service, not the tool wrapper. Investigation needed (curl with token, try other BUID, attach cookie explicitly). ~1–2h. |
| **G29** — Feedback prompt missing in claude-code mode | ⏳ Open (P2) | — | Q19 in claude-code mode | — | Discovered while closing G06. New row in `GAPS.md`. Mirror the G06 fix into `system_prompt.py` + `run_single_shot`. ~30 min. |
| **G30** — `jira_live_get_ticket` has no rate-limit backoff | ⏳ Open (P3, latent) | — | — | — | Discovered during G01 smoke test. Real Atlassian budget ~400/window; pilot won't hit it. Add token-bucket if telemetry shows runaway loops. |
| **G15** — Tool-trace visibility in chat UI | ✅ Closed | 2026-05-25 | — (frontend UX) | — (manual: collapsible "Evidence gathering" panel renders per assistant message) | Already shipped before this pass — discovery confirmed `ask.ts:108–143` renders the full trace panel with source-type badges, round number, input/output preview, and ok/error status. Backend `QueryResponse.tool_trace` wired through to the frontend. |
| **Feedback UX duplicate footer** — Answer ID block rendered twice | ✅ Closed | 2026-05-25 | — (frontend UX) | — (manual: load any answer, footer rendered once via FeedbackPanel only) | `ask.ts:renderMarkdown` now strips the `\n---\n**Review this answer:** ... **Answer ID:** ...` block from incoming markdown before rendering. The structured `<app-feedback-panel>` widget handles scoring/labels/correction; the markdown duplicate is gone. |
| **G08 frontend half** — "Need:" clarification affordance | ✅ Closed | 2026-05-25 | Q11 (PARTIAL bridged earlier; now the binary `.com or .in?` form auto-renders) | — (manual: simulate by injecting `**Need:** .com or .in?` into a conversation; both button paths and free-text fallback verified via build + template tests) | `parseNeedClarification` matches `**Need:** <q>` suffix, classifies as `binary` (regex `\b<slug>\s+or\s+<slug>\?`) or `text`. Binary renders two buttons; text renders a single input. Selection constructs a follow-up "<original>. <choice>." and auto-submits via the existing `ask()` flow. Footer also stripped from the rendered body via `stripNeedFooter`. |
| **Operational context banner** — Stale-mirror + admin queue warning on chat page | ✅ Closed | 2026-05-25 | — (frontend UX; closes the per-user half of G04) | `tests/test_status_endpoint.py` (4 tests: auth required, viewer omits admin field, admin sees pending count, missing-sync graceful) | New `GET /status` endpoint (`_require_user`-gated) returns `jira_mirror_age_hours`, `last_successful_sync`, `wiki_page_count`, and (admins only) `pending_admin_review_count`. Frontend `ask.ts` polls on load and renders a dismissible amber/info banner when Jira mirror >36h OR admin has pending reviews. Dismissal persists per-session via `sessionStorage`. |
| **Proposal review UI** — Track A admin surface in the dashboard | ✅ Closed | 2026-05-25 | — (frontend UX; closes the admin half of Track A) | — (manual: list filters, type-specific detail views, Apply / Mark Applied / Reject all rendered correctly) | New standalone component `WikiProposalsPanel` mounted into `admin-dashboard.ts`. Features: status filter (pending / applied / rejected / all), type badges (new / edit / append / multi_edit / legacy_text), per-type detail body (new = full content; edit = old/new pair; append = appended block; multi_edit = per-edit pair; legacy_text = banner + free text), `suggested_companion_edit` sidebar callout, `validation_log` collapsible, Apply / Mark Applied / Reject actions with hint messages for `stale_proposal` / `write_io_error` / `legacy_text_refused` / `already_applied`. Diff highlighting deferred (raw old/new content is rendered with red-tinted / green-tinted backgrounds, but no line-level diff). 4 new API methods in `api.service.ts`: `listProposals`, `applyProposal`, `markProposalApplied`, `rejectProposal`. |
| **Single-key deployment enforcement** — ANTHROPIC_API_KEY is the only source | ✅ Closed | 2026-05-25 | — (deployment model) | `tests/test_query_single_key_auth.py` (5 tests) + reshaped `tests/test_auth.py` resolve_api_key tests (4 tests) | `resolve_api_key` in `config.py` ignores any request_key argument. `QueryRequest` has `model_config = {"extra": "forbid"}` and the `claude_api_key` field removed — Pydantic returns 422 for any caller still sending it. `/query` auth guard is now unconditional (api mode requires Bearer token always). Missing `ANTHROPIC_API_KEY` returns 503 with user-facing message ("This deployment is missing an API key. Contact your admin."). Lifespan logs a WARN at startup if the env var is unset. |
| **Frontend per-user-key UI removed** | ✅ Closed | 2026-05-25 | — (frontend UX) | — (build clean; `ApiKeyInput` component deleted) | Removed `ApiKeyInput` component entirely (`shared/api-key-input/` directory deleted). Removed `claude_api_key` field from frontend `QueryRequest` interface and request payload. Removed `apiKey` state, `onApiKeyChange`, `clearApiKey`, `getStoredApiKey`, `setApiKey`, and the `conwo_anthropic_key` localStorage key. `canAsk()` now checks `hasServerKey()` only for api mode. New error message when server has no key: "This deployment is missing an API key. Contact your admin." |
| **Auth interceptor** — single source of bearer-token attachment | ✅ Closed | 2026-05-25 | — (fixes Issue 1 root cause + prevents recurrence) | — (manual: `/conversations`, `/query`, `/admin/*` all receive header automatically when token is in localStorage) | New functional interceptor at `frontend/src/app/core/auth.interceptor.ts`. Reads `conwo_admin_token` from localStorage; attaches `Authorization: Bearer ${token}` to every outgoing request EXCEPT `/health` and `/health/claude-code`. Preserves any explicit Authorization header that a caller already set, so existing manual-attachment patterns (query, streamQuery, getStatus, adminHeaders) continue working unchanged. Registered via `withInterceptors([authInterceptor])` in `app.config.ts`. |
| **Login UI + route guard** | ✅ Closed | 2026-05-25 | — (pilot UX) | — (manual: see Phase 5 verification in batched report) | New `/login` route with single password-type input ("Access token") and "Sign in" button. Submit validates the token against `GET /status` with the header attached — 200 → stores token + redirects to `/ask`; 401 → "Access denied — check your token or contact your admin." Functional `authGuard` (CanActivateFn) at `core/auth.guard.ts` applied to /ask, /search, /admin — redirects to /login if no token in localStorage. "Sign out" button in the top-bar nav clears localStorage + redirects to /login (hidden when not signed in or on /login). Admin-dashboard "Connect" token form REMOVED — login is now the single auth surface. Helper text on login page: "Don't have a token? Contact your admin." |
| **Rename `getAdminToken` → `getAuthToken`** | ⏳ Open (P3, cosmetic) | — | — | — | The localStorage key `conwo_admin_token` and methods `getAdminToken`/`setAdminToken` are used for EVERY user's bearer token, not just admins. The naming dates back to when admin was the only authenticated role. Rename + migrate the localStorage key when ergonomically convenient; functional impact is zero. |
