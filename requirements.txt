# Phase 1 — Tier 0 Jira sync pipeline
requests==2.32.3
openpyxl==3.1.5
tenacity==9.0.0
python-dotenv==1.0.1
truststore==0.10.1

# Phase 3 — Triage classifier (uncomment when needed)
# anthropic==0.39.0

# Phase 5 — Embeddings + clustering (uncomment when needed)
# sentence-transformers==3.0.1
# sqlite-vec==0.1.6
# hdbscan==0.8.38
# numpy==1.26.4
