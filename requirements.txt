# Phase 1 — Tier 0 Jira sync pipeline
requests==2.32.3
openpyxl==3.1.5
tenacity==9.0.0
python-dotenv==1.0.1
truststore==0.10.1
pdfplumber==0.11.4
python-docx==1.1.2
python-pptx==1.0.2

# Phase 3 — Triage classifier (uncomment when needed)
# anthropic==0.39.0

# Retrieval v2 — semantic + hybrid search (retrieval-v2 landed 2026-07)
# Live: Gemini embeddings + bge-reranker (via sentence-transformers) + pgvector.
google-genai>=2.10.0
sentence-transformers>=3.0.0
pgvector>=0.3.0

# Phase-5 clustering / synthesis stubs (unused; keep as reference for future work)
# sqlite-vec==0.1.6
# hdbscan==0.8.38
# numpy==1.26.4
